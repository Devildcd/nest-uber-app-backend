import { WalletMovementsEntity } from './wallet-movement.entity';

describe('WalletMovementsEntity', () => {
  it('should be defined', () => {
    expect(new WalletMovementsEntity()).toBeDefined();
  });
});
